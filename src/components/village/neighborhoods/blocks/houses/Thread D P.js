import React from 'react';
import ReactDOM from 'react-dom';

export default class ThreadDP extends React.Component {
       render() {
         return (
           <section className="thread-d-p">
               
               <img width={70} height={70} src={ require ('./images/web designer.jpg') } alt="thumbnail"/>
           </section>
         )
       }
  }
